<div class="container">
  <h2>Doing</h2>
    <div>
      <%= attachment_image_tag current_user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg" %>
      <button type="button" class="btn btn-secondary rounded-circle p-0" style="width:2rem;height:2rem;">
        <i class="fas fa-user-edit"></i>
      </button>
    </div>
    <div>
      <%= form_with model: @now_new, local:true do |f| %>
        <%= f.text_field :now_body %><br>
        <%= f.select :hobby_code, options_for_select({ "スポーツ" => "スポーツ", "プログラミング" => "プログラミング", "ゲーム" => "ゲーム", "料理" => "料理", "食事" => "食事", "読書" => "読書", "勉強" => "勉強", "映画" => "映画", "イベント" => "イベント", "その他" => "その他"}) %><br>
        <%= f.text_field :address %><br>
        <%= f.submit '趣meets!' %>
      <% end %>
    </div>
</div>

<div class="container">
<h2>Timeline</h2>
<table class="table">
  <thead>
    <tr>
      <td>User</td>
      <td>Doing</td>
      <td>Category</td>
      <td>Place</td>
      <td></td>
      <td></td>
      <td></td>
      <td></td>
    </tr>
  </thead>
  <tbody>
    <% @nows.each do |now| %>
    <tr>
      <td>
        <%= attachment_image_tag now.user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg" %><br>
        <%=link_to now.user.name, user_path(now.user) %>
        <%= time_ago_in_words(now.created_at) %>
      </td>
      <td><%= now.now_body %></td>
      <td><%= now.hobby_code %></td>
      <td>@ <%= now.address %></td>
      <% if now.favorited_by?(current_user) %>
      <td>
        <%= link_to now_favorites_path(now), method: :delete do %>
          <i class="fas fa-thumbs-up"></i>️<%= now.favorites.count %>
        <% end %>
      </td>
      <% else %>
      <td>
        <%= link_to now_favorites_path(now), method: :post do %>
          <i class="far fa-thumbs-up"></i>️<%= now.favorites.count %>
        <% end %>
      </td>
      <% end %>
      <td>
        <%= link_to now_reply_path(now.user.id, now.id) do %>
          💭
        <% end %>
      </td>
      <td>
      <% if current_user == now.user %>
       <%= link_to "Edit", edit_now_path(now.id) %>
       <%= link_to "Destroy", now_path(now.id), method: :delete %>
      <% end %>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>
<%= paginate @nows %>
</div>
